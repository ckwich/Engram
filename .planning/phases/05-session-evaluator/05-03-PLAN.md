---
phase: 05-session-evaluator
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - config.json
  - C:/Users/colek/.claude/settings.json
autonomous: false
requirements:
  - EVAL-06
  - EVAL-10

must_haves:
  truths:
    - "config.json has a session_evaluator section with default logic_win_triggers, milestone_triggers, auto_approve_threshold"
    - "settings.json Stop hooks array has the Engram hook appended (require_summary.py hook preserved as first entry)"
    - "The hook command uses absolute venv Python path C:/Dev/Engram/venv/Scripts/python.exe"
    - "Running the stop hook smoke test against a non-active payload exits 0 and produces a log entry in .engram/evaluator.log"
  artifacts:
    - path: "config.json"
      provides: "Global session_evaluator defaults"
      contains: "session_evaluator"
    - path: "C:/Users/colek/.claude/settings.json"
      provides: "Hook registration — Engram stop hook appended to Stop array"
      contains: "engram_stop.py"
  key_links:
    - from: "C:/Users/colek/.claude/settings.json Stop hooks"
      to: "C:/Dev/Engram/hooks/engram_stop.py"
      via: "absolute venv python command"
      pattern: "C:/Dev/Engram/venv/Scripts/python.exe C:/Dev/Engram/hooks/engram_stop.py"
---

<objective>
Wire the session evaluator into the system: add session_evaluator defaults to config.json and register the Stop hook in settings.json.

Purpose: Without hook registration, the evaluator never fires. Without config defaults, per-project config has no fallback. This plan makes the evaluator always-on for every session (EVAL-10).
Output: Updated config.json with session_evaluator section; updated settings.json with hook appended to Stop array.
</objective>

<execution_context>
@C:/Users/colek/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/colek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-session-evaluator/05-CONTEXT.md
@.planning/phases/05-session-evaluator/05-RESEARCH.md
@.planning/phases/05-session-evaluator/05-01-SUMMARY.md

<interfaces>
<!-- Current state of files to be modified -->

Current config.json (C:/Dev/Engram/config.json):
```json
{
  "dedup_threshold": 0.92,
  "stale_days": 90
}
```

Current settings.json Stop hooks array (C:/Users/colek/.claude/settings.json):
```json
"Stop": [
  {
    "matcher": "",
    "hooks": [
      {
        "type": "command",
        "command": "python C:/Users/colek/.claude/Hooks/require_summary.py"
      }
    ]
  }
]
```

Target config.json session_evaluator section (from CONTEXT.md D-14):
```json
"session_evaluator": {
  "logic_win_triggers": [
    "bug resolved",
    "new capability added",
    "architectural decision made"
  ],
  "milestone_triggers": [
    "phase completed",
    "feature shipped",
    "significant refactor done"
  ],
  "auto_approve_threshold": 0.0
}
```

Target settings.json Stop hooks array (require_summary.py must remain as first entry):
```json
"Stop": [
  {
    "matcher": "",
    "hooks": [
      {
        "type": "command",
        "command": "python C:/Users/colek/.claude/Hooks/require_summary.py"
      }
    ]
  },
  {
    "matcher": "",
    "hooks": [
      {
        "type": "command",
        "command": "C:/Dev/Engram/venv/Scripts/python.exe C:/Dev/Engram/hooks/engram_stop.py"
      }
    ]
  }
]
```

CRITICAL: Use absolute venv Python path in settings.json command (not bare "python") — Stop hook environment does not inherit PATH on Windows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session_evaluator defaults to config.json</name>
  <files>config.json</files>
  <read_first>
    - .planning/phases/05-session-evaluator/05-CONTEXT.md (D-13, D-14)
    - config.json (current content — read before modifying)
  </read_first>
  <action>
Read C:/Dev/Engram/config.json (current content: dedup_threshold and stale_days only).

Add the session_evaluator section with these exact defaults (per D-14):

```json
{
  "dedup_threshold": 0.92,
  "stale_days": 90,
  "session_evaluator": {
    "logic_win_triggers": [
      "bug resolved",
      "new capability added",
      "architectural decision made"
    ],
    "milestone_triggers": [
      "phase completed",
      "feature shipped",
      "significant refactor done"
    ],
    "auto_approve_threshold": 0.0
  }
}
```

Preserve existing keys exactly (dedup_threshold: 0.92, stale_days: 90). Write with 2-space indent. This becomes the global fallback when a project has no .engram/config.json session_evaluator section.
  </action>
  <verify>
    <automated>C:/Dev/Engram/venv/Scripts/python.exe -c "import json; c=json.load(open('C:/Dev/Engram/config.json')); assert 'session_evaluator' in c; assert c['session_evaluator']['auto_approve_threshold']==0.0; assert len(c['session_evaluator']['logic_win_triggers'])==3; print('config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "session_evaluator" C:/Dev/Engram/config.json returns a match
    - grep "auto_approve_threshold" C:/Dev/Engram/config.json returns a match
    - grep "dedup_threshold" C:/Dev/Engram/config.json returns a match (existing key preserved)
    - Python validation: json.load succeeds, session_evaluator.auto_approve_threshold == 0.0, logic_win_triggers has 3 entries
  </acceptance_criteria>
  <done>config.json has session_evaluator section with 3 logic_win_triggers, 3 milestone_triggers, auto_approve_threshold=0.0. Existing keys preserved. Valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Register Stop hook in ~/.claude/settings.json</name>
  <files>C:/Users/colek/.claude/settings.json</files>
  <read_first>
    - .planning/phases/05-session-evaluator/05-CONTEXT.md (D-01, D-02)
    - .planning/phases/05-session-evaluator/05-RESEARCH.md (Pattern 4: Settings.json Hook Registration, Pitfall 1: Stop Hook Infinite Loop, Anti-Patterns to Avoid)
    - C:/Users/colek/.claude/settings.json (read full current content before modifying — CRITICAL)
  </read_first>
  <action>
Read the full C:/Users/colek/.claude/settings.json before making any changes.

Locate the "Stop" array within "hooks". It currently has one entry (require_summary.py). APPEND a second entry. Do NOT remove or reorder the existing entry — require_summary.py must remain first.

The new entry to append:
```json
{
  "matcher": "",
  "hooks": [
    {
      "type": "command",
      "command": "C:/Dev/Engram/venv/Scripts/python.exe C:/Dev/Engram/hooks/engram_stop.py"
    }
  ]
}
```

CRITICAL: The command must use the absolute venv Python path `C:/Dev/Engram/venv/Scripts/python.exe` — not bare `python`. The Stop hook environment does not inherit PATH on Windows (same lesson as INDX-12 for git hooks).

Write the full updated settings.json preserving all other sections exactly: env, includeCoAuthoredBy, PreToolUse hooks, PostToolUse hooks, SessionStart hooks, statusLine, enabledPlugins, effortLevel, skipDangerousModePermissionPrompt.

Use 2-space indent. Ensure valid JSON — parse with json.load before writing to verify.
  </action>
  <verify>
    <automated>C:/Dev/Engram/venv/Scripts/python.exe -c "import json; s=json.load(open('C:/Users/colek/.claude/settings.json')); stops=s['hooks']['Stop']; assert len(stops)==2; assert 'engram_stop.py' in stops[1]['hooks'][0]['command']; assert 'require_summary' in stops[0]['hooks'][0]['command']; print('settings OK, Stop hooks:', len(stops))"</automated>
  </verify>
  <acceptance_criteria>
    - settings.json remains valid JSON (json.load succeeds)
    - Stop hooks array has exactly 2 entries
    - First entry still contains require_summary.py (unchanged)
    - Second entry contains "engram_stop.py" in the command
    - Second entry command uses "C:/Dev/Engram/venv/Scripts/python.exe" (absolute path)
    - All other settings.json sections (PreToolUse, PostToolUse, SessionStart, statusLine, enabledPlugins) are unchanged
  </acceptance_criteria>
  <done>settings.json has 2 Stop hooks: require_summary.py first (unchanged), engram_stop.py second with absolute venv Python path. All other sections preserved. Valid JSON.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete session evaluator system:
    - hooks/engram_stop.py — stop hook entry point
    - hooks/engram_evaluator.py — detached evaluator worker
    - ~/.claude/skills/engram-pending/SKILL.md — pending drafts approval skill
    - config.json — session_evaluator defaults added
    - ~/.claude/settings.json — Engram Stop hook registered
  </what-built>
  <how-to-verify>
    1. Verify settings.json hook registration:
       ```
       C:/Dev/Engram/venv/Scripts/python.exe -c "import json; s=json.load(open('C:/Users/colek/.claude/settings.json')); print([h['hooks'][0]['command'] for h in s['hooks']['Stop']])"
       ```
       Expected: two commands — require_summary.py and engram_stop.py

    2. Smoke test the stop hook (non-active session):
       ```
       echo '{"session_id":"test-123","cwd":"C:/Dev/Engram","stop_hook_active":false,"last_assistant_message":"Implemented the session evaluator hook. The evaluator spawns as a detached subprocess."}' | C:/Dev/Engram/venv/Scripts/python.exe C:/Dev/Engram/hooks/engram_stop.py
       echo "exit code: $?"
       ```
       Expected: exits 0, evaluator.log updated at C:/Dev/Engram/.engram/evaluator.log

    3. Smoke test loop-prevention (stop_hook_active=true):
       ```
       echo '{"session_id":"test-456","cwd":"C:/Dev/Engram","stop_hook_active":true,"last_assistant_message":""}' | C:/Dev/Engram/venv/Scripts/python.exe C:/Dev/Engram/hooks/engram_stop.py
       echo "exit code: $?"
       ```
       Expected: exits 0 immediately, NO new evaluator.log entry

    4. Check evaluator log after step 2 (wait ~30 seconds for detached process):
       ```
       cat C:/Dev/Engram/.engram/evaluator.log
       ```
       Expected: log entry showing evaluation result or pending file write

    5. Check for pending files (if evaluator found the test message worth capturing):
       ```
       ls C:/Dev/Engram/.engram/pending_memories/ 2>/dev/null && echo "pending files found" || echo "no pending files (evaluator said not worth capturing)"
       ```
       Either outcome is acceptable — the evaluator determines worth_capturing.
  </how-to-verify>
  <resume-signal>Type "approved" if both hooks exit 0 and the loop-prevention test produces no log entry. Describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
End-to-end checklist:
- [ ] config.json has session_evaluator section with correct defaults
- [ ] settings.json Stop array has 2 entries, require_summary.py first, engram_stop.py second
- [ ] Both hook smoke tests exit 0
- [ ] stop_hook_active=true test produces no evaluator subprocess (loop prevention confirmed)
- [ ] evaluator.log created at C:/Dev/Engram/.engram/evaluator.log after non-active test
</verification>

<success_criteria>
- config.json: session_evaluator section with 3 logic_win_triggers, 3 milestone_triggers, auto_approve_threshold=0.0
- settings.json: 2 Stop hooks, require_summary.py first (unchanged), engram_stop.py second with absolute venv path
- Human verification: both smoke tests pass, loop prevention confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/05-session-evaluator/05-03-SUMMARY.md`
</output>
